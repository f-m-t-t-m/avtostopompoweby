{% extends 'layouts/base.twig' %}

{% set scope = {
    section: section,
    comments: comments,
    users: users,
    replies: replies,
} %}

{% block main %}
    <div class="ContainerSection">
        {% include 'components/study-section-post.twig' with {
            scope: scope,
        } %}
    </div>
{% endblock %}
